const {
    interpolatePixels,
    interpolateTwoLayers,
    blendPixels,
    blendTwoPixels,
    blendLayers
} = require('./build/Release/LedServerUtilities.node');

module.exports = {
    interpolatePixels,
    interpolateTwoLayers,
    blendPixels,
    blendTwoPixels,
    blendLayers
};
