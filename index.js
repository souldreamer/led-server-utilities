const {
    interpolatePixels,
    interpolateTwoLayers,
    blendPixels,
    blendTwoPixels,
    blendLayers
} = require('./build/Release/LedServerUtilities.node');

export {
    interpolatePixels,
    interpolateTwoLayers,
    blendPixels,
    blendTwoPixels,
    blendLayers
};
